<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jav.server.dao.relation.MovieActressDao">
    
    <!-- MovieActressRelation resultMap -->
    <resultMap id="movieActressRelationResultMap" type="com.jav.server.entity.relation.MovieActressRelation">
        <id property="id" column="id" />
        <result property="movieId" column="movie_id" />
        <result property="actressId" column="actress_id" />
    </resultMap>
    
    <insert id="addMovieActressRelation" parameterType="com.jav.server.entity.relation.MovieActressRelation">
        INSERT INTO movie_actress_relation (movie_id, actress_id) 
        VALUES (#{movieId}, #{actressId})
    </insert>

    <delete id="deleteMovieActressRelation">
        DELETE FROM movie_actress_relation 
        WHERE movie_id = #{movieId} AND actress_id = #{actressId}
    </delete>
    
    <!-- 批量添加电影与女优关系 -->
    <insert id="addMovieActressRelations" parameterType="java.util.List">
        INSERT INTO movie_actress_relation (movie_id, actress_id) VALUES
        <foreach collection="list" item="relation" separator=",">
            (#{relation.movieId}, #{relation.actressId})
        </foreach>
    </insert>
    
    <select id="queryMovieActressRelation" resultMap="movieActressRelationResultMap">
        SELECT * FROM movie_actress_relation 
        WHERE movie_id = #{movieId} AND actress_id = #{actressId}
    </select>

    <select id="queryMovieActressRelations" resultMap="movieActressRelationResultMap">
        SELECT * FROM movie_actress_relation 
        WHERE movie_id = #{movieId} AND actress_id IN 
        <foreach collection="actressIds" item="actressId" open="(" separator="," close=")">
            #{actressId}
        </foreach>
    </select>

    <!-- 查询某电影所有女优关系 -->
    <select id="queryMovieActressRelationByMovieId" resultMap="movieActressRelationResultMap">
        SELECT * FROM movie_actress_relation 
        WHERE movie_id = #{movieId}
    </select>
        <!-- 更新电影女优关系 -->
    <update id="updateMovieActressRelations" parameterType="java.util.List">
        <foreach collection="relations" item="relation" separator=";">
            UPDATE movie_actress_relation
            SET movie_id = #{relation.movieId}, actress_id = #{relation.actressId}
            WHERE id = #{relation.id}
        </foreach>
    </update>
</mapper>
