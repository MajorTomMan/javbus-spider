<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.javbus.spider.spider.dao.StarDao">

    <!-- 插入操作 -->
    <insert id="save" parameterType="com.javbus.spider.spider.entity.Star">
        INSERT INTO star(
            name, star_link, photo_link, birth_day, age, height, cup, bust, waist, hip, birth_place, hobby
        )
        VALUES (
            #{name}, #{starLink}, #{photoLink}, #{birthDay}, #{age}, #{height}, #{cup}, #{bust}, #{waist}, #{hip}, #{birthPlace}, #{hobby}
        )
    </insert>

    <!-- 删除操作 -->
    <delete id="delete" parameterType="int">
        DELETE FROM star WHERE id = #{id}
    </delete>

    <!-- 查询操作 -->
    <select id="queryStarById" parameterType="int" resultType="com.javbus.spider.spider.entity.Star">
        SELECT * FROM star WHERE id = #{id}
    </select>

    <!-- 更新操作 -->
    <update id="update" parameterType="com.javbus.spider.spider.entity.Star">
        UPDATE star
        SET
            name = #{name},
            star_link = #{starLink},
            photo_link = #{photoLink},
            birth_day = #{birthDay},
            age = #{age},
            height = #{height},
            cup = #{cup},
            bust = #{bust},
            waist = #{waist},
            hip = #{hip},
            birth_place = #{birthPlace},
            hobby = #{hobby}
        WHERE id = #{id}
    </update>

</mapper>