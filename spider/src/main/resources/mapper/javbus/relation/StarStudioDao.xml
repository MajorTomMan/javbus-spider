<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.javbus.spider.spider.dao.relation.StarStudioDao">

    <insert id="addStarStudioRelation" parameterType="com.javbus.spider.spider.entity.relation.StarStudioRelation">
        INSERT IGNORE INTO star_studio_relation (star_id, studio_id)
        VALUES (#{starId}, #{studioId})
    </insert>

    <delete id="deleteStarStudioRelation">
        DELETE FROM star_studio_relation
        WHERE star_id = #{starId} AND studio_id = #{studioId}
    </delete>
    <!-- 批量添加明星与工作室关系 -->
    <insert id="addStarStudioRelations" parameterType="java.util.List">
        INSERT IGNORE INTO star_studio_relation (star_id, studio_id)
        VALUES
        <foreach collection="list" item="relation" separator=",">
            (#{relation.starId}, #{relation.studioId})
        </foreach>
    </insert>
    <select id="queryStarStudioRelation">
        SELECT * FROM star_studio_relation WHERE star_id = #{starId} AND studio_id = #{studioId}
    </select>

    <select id="queryStarStudioRelations">
        SELECT * FROM star_studio_relation WHERE star_id IN 
        <foreach collection="starIds" item="starId" open="(" separator="," close=")">
            {#starId}
        </foreach>
        AND studio_id = #{studioId}
    </select>
</mapper>
