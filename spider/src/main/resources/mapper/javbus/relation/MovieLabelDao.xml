<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javbus.spider.spider.dao.relation.MovieLabelDao">
    
    <insert id="addMovieLabelRelation" parameterType="com.javbus.spider.spider.entity.relation.MovieLabelRelation">
        INSERT IGNORE INTO movie_label_relation (movie_id, label_id)
        VALUES (#{movieId}, #{labelId})
    </insert>

    <delete id="deleteMovieLabelRelation">
        DELETE FROM movie_label_relation
        WHERE movie_id = #{movieId} AND label_id = #{labelId}
    </delete>

    <select id="queryMovieDirectorRelation" resultType="com.javbus.spider.spider.entity.relation.MovieLabelRelation">
        Select movie_label_relation
        WHERE movie_id = #{movieId} AND label_id = #{labelId}
    </select>
</mapper>